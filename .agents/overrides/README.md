# Overrides

No project-specific AI overrides were found during the Knowledge Base generation. Add narrowly scoped override files here only when a documented rule must supersede a general project convention.
